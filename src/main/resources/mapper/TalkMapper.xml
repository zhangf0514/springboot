<?xml version="1.0" encoding="UTF-8"?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN" "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.orderfood.springboot.mapper.TalkMapper">

    <select id="page" resultType="com.orderfood.springboot.entity.Talk">
        select * from talk where first=#{first} and last=#{last}
    </select>
</mapper>
